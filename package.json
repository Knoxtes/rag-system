{
  "name": "rag-system",
  "version": "2.0.0",
  "description": "7 Mountains Media RAG System - AI-powered document search and chat system for Ask.7MountainsMedia.com",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "build": "cd chat-app && npm install --legacy-peer-deps && npm run build",
    "build:frontend": "cd chat-app && ./install-and-build.sh",
    "install:deps": "npm install && cd chat-app && npm install --legacy-peer-deps",
    "install:python": "pip install --upgrade -r requirements-production.txt",
    "deploy:update": "git fetch origin && git reset --hard origin/feature/easyocr-integration",
    "deploy:full": "npm run deploy:update && npm run install:python && npm run build:frontend",
    "dev:frontend": "cd chat-app && npm start",
    "dev:backend": "python chat_api.py",
    "dev": "concurrently \"npm run dev:backend\" \"npm run dev:frontend\"",
    "health": "curl http://localhost:3000/api/health",
    "logs": "tail -f logs/rag_system.log",
    "postinstall": "echo 'Run npm run build:frontend to build React app'"
  },
  "keywords": [
    "rag",
    "ai",
    "chat",
    "document-search",
    "flask",
    "react",
    "plesk",
    "almalinux",
    "vertex-ai"
  ],
  "author": "7 Mountains Media",
  "license": "MIT",
  "homepage": "https://Ask.7MountainsMedia.com",
  "repository": {
    "type": "git",
    "url": "https://github.com/Knoxtes/rag-system.git"
  },
  "dependencies": {
    "express": "^4.18.2",
    "http-proxy-middleware": "^2.0.6"
  },
  "devDependencies": {
    "concurrently": "^7.6.0"
  },
  "engines": {
    "node": ">=18.0.0",
    "npm": ">=8.0.0",
    "python": ">=3.9.0"
  }
}