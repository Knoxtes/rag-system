{
  "name": "rag-system",
  "version": "1.0.0",
  "description": "7 Mountains Media RAG System - AI-powered document search and chat system",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "build": "cd chat-app && npm install && npm run build",
    "build:frontend": "cd chat-app && npm run build",
    "install-deps": "npm install && cd chat-app && npm install && cd .. && python3 -m pip install --user -r requirements-production.txt",
    "install:production": "npm install && cd chat-app && npm install && cd .. && python3 -m pip install --user -r requirements-production.txt",
    "install:dev": "npm install && cd chat-app && npm install && cd .. && pip install -r requirements.txt",
    "dev:frontend": "cd chat-app && npm start",
    "dev:backend": "python3 chat_api.py",
    "dev": "concurrently \"npm run dev:backend\" \"npm run dev:frontend\"",
    "deploy": "bash deploy.sh",
    "health": "node -e \"require('http').get('http://localhost:3000/api/health', r => r.on('data', d => console.log(d.toString())))\"",
    "postinstall": "cd chat-app && npm install"
  },
  "keywords": [
    "rag",
    "ai",
    "chat",
    "document-search",
    "flask",
    "react"
  ],
  "author": "7 Mountains Media",
  "license": "MIT",
  "dependencies": {
    "concurrently": "^7.6.0",
    "express": "^4.18.2",
    "http-proxy-middleware": "^2.0.6"
  },
  "engines": {
    "node": ">=25.2.0",
    "npm": ">=10.0.0",
    "python": ">=3.8.0"
  }
}